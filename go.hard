rm -rf ./build
rm -rf ./install
mkdir build
mkdir install
cd build
cmake -DCMAKE_INSTALL_PREFIX=../install \
        -DCMAKE_BUILD_TYPE=Debug \
        -DSOS_DIR=$(echo $HOME)/src/sos_flow/build \
        -DRAJA_DIR=$(spack location -i raja) \
        -DCALIPER_DIR=$(echo $HOME)/src/caliper/install \
        ..
make && make install && cd .. && tree install
